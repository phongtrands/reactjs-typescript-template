import { Box } from '@mui/material';

import PageContainer from '~/layouts/PageContainer';

const EPaymentPage = () => {
  return <Box>EPaymentPage</Box>;
};

export default PageContainer(EPaymentPage);
